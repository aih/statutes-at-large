# The .env contract of docker-compose.prod.yml, with placeholder values.
# deploy/bootstrap-box.sh writes the real file on the box (mode 600). CI
# validates the compose file against this one.
SITE_ADDRESS=http://statutes.linkedlegislation.org:8000
POSTGRES_PASSWORD=change-me
DATA_ROOT=/var/lib/statutes
ECR_REGISTRY=739065237548.dkr.ecr.us-east-1.amazonaws.com
IMAGE_TAG=latest
GOVINFO_API_KEY=
SITE_ORIGIN=https://statutes.linkedlegislation.org
USCODE_ORIGIN=https://uscode.linkedlegislation.org
BACKUP_BUCKET=statutes-linkedlegislation
