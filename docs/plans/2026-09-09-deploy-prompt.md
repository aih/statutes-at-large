# Deployment prompt

Paste from the line after the rule into a fresh session started in
`statutes-linkedlegislation`.

---

Build the deployment of statutes.linkedlegislation.org in this repository (statutes-linkedlegislation, https://github.com/aih/statutes-at-large) and its counterpart on the US Code site, following docs/plans/2026-09-08-deployment-plan.md, which is accepted with these decisions: the site runs on the US Code site's existing `t4g.large` (instance tagged `Name=uscode-site`, us-east-1, ECR registry 739065237548.dkr.ecr.us-east-1.amazonaws.com) as a second compose project behind one edge Caddy that terminates TLS for both hostnames; each application keeps its own Caddyfile, Postgres, data volume, memory limits, deploy lock, watchdog and backups; `robots.txt` answers `Disallow: /` on both sites; links between the two sites are allowed in both directions. Stages 1 to 5 are done and pushed to `main` (the reader at `/app`, `GET /api/v1/cite`, the US Code site's `statutes-links` branch, not yet merged there). Read first: CLAUDE.md, README.md, BUILDLOG.md (the stage 5 entry), docs/adr/0014 to 0016, the plan in full (sections 1, 2 and 6 are the contract for this work), docker-compose.yml, deploy/Caddyfile, Dockerfile, frontend/Dockerfile, frontend/astro.config.mjs, frontend/src/lib/api.ts, params.py (`client_key`, `rate_limit`), api/cite.py and api/cited_by.py (the per-person limits), ingest/hub.py, ingest/__main__.py, ingest/citations.py (how `--from-hub` records the dataset revision in `source_checks`), storage/repository.py (`SourceCheckInfo`, `collection_status`), Makefile. Then in ../uscode-redesign (read only until the delegation below): docs/adr/0020, 0029, 0035, 0036, 0073, docs/deploy.md in full, docker-compose.prod.yml, deploy/Caddyfile, deploy/provision.sh, deploy/bootstrap-box.sh, deploy/deploy-on-box.sh, deploy/update-corpus.sh, deploy/install-crons.sh, deploy/watchdog.sh, deploy/alarms.sh, .github/workflows/ci.yml, deploy.yml, update-corpus.yml. Keep the conventions: the API has no frontend dependency; one Repository protocol, no SQL outside storage/ and ingest/; notes and cache headers in params.py; pytest over SQLite with the committed fixtures, nothing in `make test` needing Node, a browser, a database or the network; a Makefile target per step; an ADR for every decision that departs from the plan or from the US Code site's conventions; never a token or key in source. Prose follows ~/.claude/CLAUDE.md.

Inputs (measured 2026-09-08):

- The corpus is small: 137 volumes of 5 to 45 MB on the Hub (about 2.7 GB), 8 to 14 s to load a volume, about an hour for all; PLAW 113th to 119th 63 s; the citation index 405 s (1,081,463 rows); the classification mirror 78 s; COMPS 2,685 packages at two GovInfo API calls each under 1,000 calls an hour. The dev Postgres is 792 MB with five volumes and both indexes; the full corpus is 6 to 10 GB.
- The dev stack here: docker compose with `db` on :5434, `api` on 8001 (`--reload`, a bind mount of the tree), `frontend` on 4321 reading `API_BASE_URL=http://api:8001`, `proxy` (Caddy) on :8010 with `deploy/Caddyfile`'s `{$SITE_ADDRESS::8000}` site block, `handle /app*` to the frontend and `header_up X-Forwarded-For {remote_host}` in both handle blocks. The US Code site's dev stack runs on this machine on :8000 (Postgres :5432, OpenSearch, Redis).
- The API keys its rate limiters on `request.client.host`, which uvicorn fills from the leftmost `X-Forwarded-For` under `--forwarded-allow-ips *`. The reader calls `cite` (from `/app/goto`) and `cited-by` (the panel) server-side and forwards nothing, so on the box every reader would share one bucket of 60 then 2 a second. The reader has no `/app/healthz` route; the compose file has no healthchecks and no memory limits; there is no production compose file, no deploy script, no workflow.
- The US Code site's production shape: `docker-compose.prod.yml` standalone with `proxy` publishing 80 and 443 and `SITE_ADDRESS=uscode.linkedlegislation.org`; images from ECR tagged by sha; `deploy.yml` on CI success builds on `ubuntu-24.04-arm` and runs `deploy-on-box.sh <sha>` over SSM by instance id resolved from the tag; `update-corpus.yml` weekly plus a daily cron; the watchdog every minute; `DATA_ROOT=/var/lib/uscode`; the instance role `uscode-site` with S3 on the mirror bucket only; the OIDC role `uscode-github-deploy` trusted by that repository only.
- The sources' update channels: `python -m ingest plaw poll` (listings against stored rows, 12 s when nothing is due), `comps poll --since`, `classifications` (unchanged tables skipped), `citations --from-hub` (always a full reload; the revision is recorded), `fetch-statute` (skips files present unless `--force`; no change check against the Hub).

Sequence and delegation:

Yourself first, in this repository: (1) the reader forwards the client address: `api.ts` sends `X-Forwarded-For` from `Astro.clientAddress` on every server-side call (pass the address in from the page; `fetchLabels` and the rest take it as an option), and a vitest unit proves the header; `frontend/src/pages/healthz.astro` answers 200 with no API call. (2) `deploy/Caddyfile`: `header_up X-Forwarded-For {client_ip}` in both handle blocks, with a comment saying why it is the same line on the dev stack and behind the edge. (3) The ingest flags of plan section 2 item 6: `fetch-statute --if-changed` (the Hub tree listing `https://huggingface.co/api/datasets/dreamproit/us-statutes-at-large/tree/main/xmls`, sha and size per file, compared with the stored file's recorded sha in a `source_checks` row or a sidecar; download only what changed), `statute --changed-only` (load only volumes whose file changed since their last load), `citations --from-hub --if-changed` (skip when the dataset revision equals the recorded one); each writes its `source_checks` row either way; tests over respx and the fixture directory, no network. (4) `make load-prod` and `make update-sources` targets that run the plan's section 5 sequence and the section 6 script through `docker compose -f docker-compose.prod.yml exec -T api`. (5) Update docs/plans/2026-09-08-reader-contract.md with the forwarded address and the health route. Run `make test`, `make test-web`, and the compose stack on :8010 (`docker compose up -d --build`, the proxy recreated) with a check that a forged `X-Forwarded-For` from a workstation does not reach the API's limiter and that a `/app/goto` request is keyed on the browser's address (read it from `docker compose logs api` with a temporary log line, or from a 429 after draining one address's bucket while another address still answers). Commit before delegating.

Two subagents in worktrees, neither editing `api/`, `storage/`, `db/`, `ingest/`, `params.py` or `frontend/src/lib/api.ts`:

(a) This repository's deployment files, modelled on the US Code site's with `uscode` → `statutes`: `docker-compose.prod.yml` (plan section 2 item 1: no published ports, ECR images with `build:` fallback, `db` with `shared_buffers=256MB`, `api` with `${DATA_ROOT}/data:/app/data` and `--limit-concurrency 64`, healthchecks on `/health` and `/app/healthz`, `mem_limit` 768m/512m/384m/64m, `proxy` with `SITE_ADDRESS=http://statutes.linkedlegislation.org:8000` on the external network `edge` with alias `statutes-proxy`, `restart: unless-stopped`); `deploy/edge/docker-compose.yml`, `deploy/edge/Caddyfile` (two site blocks, TLS by hostname, `reverse_proxy uscode-proxy:8000` and `statutes-proxy:8000`, `header_up X-Forwarded-For {remote_host}`, `header_up Host {host}`, certificates on `${DATA_ROOT}/edge-caddy`) and `deploy/edge/up.sh` (creates the `edge` network when absent, brings the edge up, idempotent); `deploy/provision.sh` (the 40 GB gp3 volume with `DeleteOnTermination=false` in the instance's availability zone attached to the instance tagged `Name=uscode-site`, the bucket `statutes-linkedlegislation` with a 60-day lifecycle on `db/`, the ECR repositories `statutes-api` and `statutes-frontend` with a ten-tag lifecycle; idempotent; prints what it reused), `deploy/admin-grant.sh` and its bootstrap policy JSON (the statement on the `uscode-site` role for the bucket, the OIDC role `statutes-github-deploy` trusted by `repo:aih/statutes-at-large:*` with ECR push on the two repositories, `ssm:SendCommand` and `ssm:GetCommandInvocation` on the instance, `ec2:DescribeInstances`; the smallest set of IAM actions, each scoped), `deploy/bootstrap-box.sh` (the new volume found by elimination and formatted only when it has no filesystem, mounted at `/var/lib/statutes` with `pgdata`, `data`, `caddy`, `edge-caddy`, `logs`; the clone into `/home/ec2-user/statutes-at-large`; `.env` written once and kept; `install-crons.sh`), `deploy/deploy-on-box.sh` (ECR login, `IMAGE_TAG` pinned, pull, `alembic upgrade head` with the new image before it serves, `up -d --wait`, the proxy recreated, a check through the edge with `--resolve statutes.linkedlegislation.org:443:127.0.0.1` that `robots.txt` says `Disallow: /`, prune; `flock` on `${DATA_ROOT}/deploy.lock`; log to `${DATA_ROOT}/logs/deploy.log`), `deploy/update-sources.sh` (plan section 6: the seven steps in order inside the `api` container, each recorded, `pg_dump` to `s3://${BACKUP_BUCKET}/db/statutes-<date>.dump` only when a step wrote rows, `/api/v1/status` into the log with `stale` asserted false, `--check-only` and `--force` modes, `flock` on `${DATA_ROOT}/update.lock`), `deploy/install-crons.sh` (`/etc/cron.d/statutes`: the update Thursdays 06:41 UTC, the watchdog every minute, `docker image prune -f` weekly), `deploy/watchdog.sh` (probe `https://statutes.linkedlegislation.org/health` and `/app/healthz` through the edge with `--resolve`, publish `Statutes/SiteUp`, restart only this project's `api`, `frontend` and `proxy` after three consecutive failures, never the edge), `deploy/alarms.sh` (an SNS topic and the `Statutes/SiteUp` alarm; the box's CPU, credit and disk alarms already exist, and the new volume gets a `disk_used_percent` alarm for `/var/lib/statutes`), `.github/workflows/ci.yml` (`make test` and `make test-web` on push and pull request; Node 22 and uv cached), `deploy.yml` and `update-sources.yml` (the US Code site's shape, targeting the instance tagged `Name=uscode-site`, the repository variable `AWS_DEPLOY_ROLE_ARN`, `executionTimeout` 4 hours for the update). A rehearsal on this machine, without AWS: a third compose project from `deploy/edge/` on :8020 with plain-HTTP site addresses `http://uscode.localhost:8020` and `http://statutes.localhost:8020`, this repository's dev stack joined to the `edge` network with the alias `statutes-proxy` (a compose override file for the rehearsal, not the dev file itself), the US Code site's dev proxy joined the same way if that stack is running, and a check that both hostnames answer through the edge, that a forged `X-Forwarded-For` from the workstation is replaced, and that the inner proxy passes the edge's value through. Shell scripts pass `shellcheck` if it is installed; `docker compose -f docker-compose.prod.yml config` validates with a sample `.env`. Do not touch AWS. Report every file, every command run and its output, and the decisions to record.

(b) The US Code site, on a new branch `shared-edge` from `main` in a new worktree of ../uscode-redesign (`.claude/worktrees/shared-edge`; the user's checkout is on another branch; the earlier `statutes-links` branch stays separate and unmerged): `docker-compose.prod.yml`'s `proxy` stops publishing 80 and 443, joins the external `edge` network with the alias `uscode-proxy`, and the documented `SITE_ADDRESS` becomes `http://uscode.linkedlegislation.org:8000`; `deploy/Caddyfile` uses `header_up X-Forwarded-For {client_ip}` in both handle blocks; `deploy-on-box.sh`'s robots check and `deploy/watchdog.sh`'s probe go through the edge (`--resolve <host>:443:127.0.0.1`, as now) and the watchdog restarts only that project's services; `docker-compose.prod.yml`'s `frontend` documents `STATUTES_ORIGIN` (already on `statutes-links`; do not duplicate the code, only the `.env` documentation if missing); `docs/deploy.md` gains a section "Sharing the box" describing the edge, the cut-over order (deploy this branch, bring the edge up from the statutes repository's `deploy/edge/up.sh`, check the US Code site through the edge, then deploy the statutes site) and the rollback (publish 80 and 443 on the proxy again); its ADR-0020 is amended with a dated note, not replaced; `make test` and `make test-web` there stay green (869 passed 2 skipped and 517 passed on 2026-09-08); `docker compose -f docker-compose.prod.yml config` validates. Tests with that site's vitest and pytest where the change is testable; a rehearsal against this machine's edge from (a) if that stack is up. Commit on the branch, do not push, report the diff and the decisions.

Yourself after the merge: merge (a); run the rehearsal end to end on this machine with both dev stacks and the edge (the US Code site from its `shared-edge` worktree if it can run beside the checked-out one on other ports, else the checked-out stack with the override); confirm through the edge: `/app/us/pl/81/740/s3` on the statutes hostname, `/app/us/usc/t16/s45f/c/5` on the US Code hostname, the bare citation URL redirecting a browser on each, `robots.txt` on each, a forged `X-Forwarded-For` replaced, a `cite` 429 keyed on one address while a second address still answers; `make test`, `make test-web`, `make test-e2e` over the edge; write ADR-0017 (one box, two applications, one edge: what is shared and what is not) and ADR-0018 (the weekly update: what each step asks its source, the two schedules, the dump gated on writes); README ("Running it" gains the production commands, a "Deployment" section pointing at the plan and the runbook); BUILDLOG; commit and push. Then stop and report: the AWS steps (plan section 3), the box steps (section 4), the load (section 5) and the cut-over need the user's go-ahead and an identity that can create IAM resources; list them in order with the exact commands, and run them only when told, one section at a time, reporting the output of each before the next.

Constraints: nothing in `make test` needs Node, a browser, a database or the network; the reader keeps zero build-time dependency on the database; the API keeps zero frontend dependencies; no token, key or password in source (the `.env` on the box is the only place; `GOVINFO_API_KEY` is not a GitHub secret); the sibling repository is changed on a branch and not pushed; nothing touches AWS until the user says so; the dev stacks on :8000, :8010, :5432 and :5434 stay usable after the rehearsal (the override and the edge project are torn down with `docker compose down`, not the dev stacks). Report faithfully at the end of each step: what passed, what failed with the output, what was skipped.
